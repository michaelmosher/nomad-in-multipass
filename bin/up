#!/usr/bin/env bash
set -euo pipefail

instanceNamePrefix="nomad-test"
customization="--memory 1g --disk 20g"

for name in "$instanceNamePrefix"-{1,2,3}; do
    multipass launch $customization --name "$name" --cloud-init user-data jammy
    multipass info "$name"
done
